export interface ConvertSetting {
  bitrate: number;
  fps: number;
  speed: number;
  time: [number, number];
}

export interface VideoInfo {
  duration: number;
  width: number;
  height: number;
}
